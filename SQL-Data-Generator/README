If you want to change the time period, you have to change it in every file (Gxx-...) in the variables.

1. Execute 01-gen-data.py only, i.e. `python 01-gen-data.py`.
2. The results are SQL scripts in `data` directory.
3. Execute these SQL script in the database.
